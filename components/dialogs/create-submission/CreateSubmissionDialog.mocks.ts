import { ICreateSubmissionDialog } from './CreateSubmissionDialog';

const base: ICreateSubmissionDialog = {
  assignmentId: '',
  classroomId: '',
};

export const mockCreateSubmissionDialogProps = {
  base,
};
