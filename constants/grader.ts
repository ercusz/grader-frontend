export const enum GraderConfig {
  // The minimum number of address space limit of the program in kilobytes.
  MIN_MEMORY_LIMIT = 1,
  // The maximum number of address space limit of the program in kilobytes.
  MAX_MEMORY_LIMIT = 100000,
  // The minimum number of runtime limit for the program in seconds.
  MIN_TIME_LIMIT = 1,
  // The maximum number of runtime limit for the program in seconds.
  MAX_TIME_LIMIT = 10,
}
